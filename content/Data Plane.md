The structure of the *data plane* within the [[Network Layer]] is: 

It's a *local* per-router function and determines how the datagram that's arriving is forwarded to the router's output interface. 

