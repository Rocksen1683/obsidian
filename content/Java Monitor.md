Java has *synchronized* class members and a *synchronized* statement. 

All of them have *one* [[uCondition]] and these methods that use it:
- `wait()`
- `notify()`
- `notifyAll()`


USE GENERATION to avoid *spurious wakeup*
![[Pasted image 20241208135014.png]]
