*OSPF* routing uses [[Link State Algorithm]] for routing and is a ISO Standard.  

Each OSPF Router floods link state (directly over [[Internet Protocol]] rather than [[Transmission Control Protocol]]) to all other routers in the entire AS. 

Uses multiple *link costs* link bandwidth and delay and all OSPF messages are *authenticated*.
![[Pasted image 20241203174622.png]]
