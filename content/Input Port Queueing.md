If the [[Switching Fabric]] is slower than the input ports combined, then *queueing* can occur at input queues which could lead to [[Packet Queuing and Loss]] and input buffer overflow.

*Output Port Contention:* Only one red datagram can be transferred 

![[Pasted image 20241203161609.png]]