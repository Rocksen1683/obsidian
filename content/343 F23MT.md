### Question 4 
```
_Coroutine Bead{
	bool zombie = false; 

	suspend(); //get the next bead 

	try{
		_Enable{
			unsigned int key = prng();
			clasp.lock(key);
		}
	}
	_CatchResume(Clasp::Zombie &){
		zombie = true;
	}
	catch(Clasp::Won &){
		cout << (zombie ? "Zombie cannot win " : "Won ") << id << endl;
	}

}


int main(){
	int N = 5;
	Bead[N] beads; //initializing beads 

	for (int i = 0; i < N; i += 1){
		beads[i].ctor(i, &beads[(i+1) % N]);
	}

	//start 
	beads[0]->next;
}
```

## Question 5 
### Part (a)
You can't use `COFOR` in this as there's no mechanism to actually control the stoppage of `COFOR` based on some condition. 

### Part (b)
```
_Actor Manager{
	N = 10; 
	uArray(Halfway, halfway, N);
	bool completed[N] = {};

	Allocation receive(Message &msg){
		case (Progress, msg){
			if () {
			//check whether you need to deduct 50
			}
			if (completed[msg_d->id] && msg_d->complWork < 1000) {
				*halfway[msg_d->id | uActor::startMsg;
			}
			else{
				
			}	
		}
		
	}

	public:
	Manager(){
		for (int i = 0; i < N; i += 1){
			halfway[i].ctor(i, *this);
		}
	}

}

int main(){
	uActor::start();
	Manager manager;
	uActor::stop();
}

```